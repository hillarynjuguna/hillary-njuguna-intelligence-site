---
import { SITE } from '@data/site';
import { FOOTER_LINKS } from '@data/navigation';

const year = new Date().getFullYear();
---

<footer class="footer">
  <div class="container">
    <div class="footer__grid">
      <div class="footer__brand">
        <a href="/" class="footer__logo">Oscillatory Fields</a>
        <p class="footer__tagline">
          Intelligence architecture for institutions that can't afford to get it wrong.
        </p>
        <p class="footer__origin label">
          Hillary Njuguna · Nairobi to Kuala Lumpur to everywhere
        </p>
      </div>

      <div class="footer__nav-group">
        <p class="footer__nav-label label">About</p>
        <nav>
          {FOOTER_LINKS.about.map(({ label, href }) => (
            <a href={href} class="footer__link">{label}</a>
          ))}
        </nav>
      </div>

      <div class="footer__nav-group">
        <p class="footer__nav-label label">Research</p>
        <nav>
          {FOOTER_LINKS.research.map(({ label, href }) => (
            <a href={href} class="footer__link">{label}</a>
          ))}
        </nav>
      </div>

      <div class="footer__nav-group">
        <p class="footer__nav-label label">Tools</p>
        <nav>
          {FOOTER_LINKS.tools.map(({ label, href }) => (
            <a href={href} class="footer__link">{label}</a>
          ))}
        </nav>
      </div>

      <div class="footer__nav-group">
        <p class="footer__nav-label label">Products</p>
        <nav>
          {FOOTER_LINKS.products.map(({ label, href }) => (
            <a href={href} class="footer__link">{label}</a>
          ))}
        </nav>
      </div>

      <div class="footer__nav-group">
        <p class="footer__nav-label label">External</p>
        <nav>
          {FOOTER_LINKS.external.map(({ label, href }) => (
            <a href={href} class="footer__link" target="_blank" rel="noopener noreferrer me">
              {label} ↗
            </a>
          ))}
        </nav>
      </div>
    </div>

    <div class="footer__bottom">
      <p class="footer__copy label">
        © {year} {SITE.author.name} · Oscillatory Fields
      </p>
      <div class="footer__meta">
        <a href="/rss.xml" class="footer__meta-link label">RSS</a>
        <a href="/sitemap-index.xml" class="footer__meta-link label">Sitemap</a>
        <a href="/clauses" class="footer__meta-link label">Governance</a>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--border-dim);
    background: var(--surface-1);
    padding: var(--space-16) 0 var(--space-8);
    margin-top: var(--space-24);
  }

  .footer__grid {
    display: grid;
    grid-template-columns: 2fr repeat(5, 1fr);
    gap: var(--space-10);
    margin-bottom: var(--space-12);
  }

  @media (max-width: 1100px) {
    .footer__grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .footer__brand {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 700px) {
    .footer__grid {
      grid-template-columns: 1fr;
    }
  }

  .footer__logo {
    display: block;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--text-primary);
    text-decoration: none;
    letter-spacing: 0.06em;
    margin-bottom: var(--space-4);
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer__logo:hover {
    color: var(--ember);
  }

  .footer__tagline {
    color: var(--text-secondary);
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-3);
    max-width: 36ch;
  }

  .footer__origin {
    color: var(--text-tertiary);
  }

  .footer__nav-label {
    margin-bottom: var(--space-4);
    color: var(--ember);
  }

  .footer__nav-group nav {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .footer__link {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.05em;
    color: var(--text-tertiary);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
    line-height: 1.5;
  }

  .footer__link:hover {
    color: var(--text-secondary);
  }

  .footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: var(--space-6);
    border-top: 1px solid var(--border-dim);
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .footer__copy {
    color: var(--text-tertiary);
  }

  .footer__meta {
    display: flex;
    gap: var(--space-5);
    flex-wrap: wrap;
  }

  .footer__meta-link {
    color: var(--text-tertiary);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .footer__meta-link:hover {
    color: var(--ember);
  }
</style>
