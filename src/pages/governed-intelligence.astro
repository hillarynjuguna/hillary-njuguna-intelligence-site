---
export const prerender = true;
import FieldShellLayout from '@layouts/FieldShellLayout.astro';
import { buildFieldPrompt } from '@lib/field';
import MAKernelGraph from '@components/ui/MAKernelGraph.astro';
import ReversibilityLadder from '@components/ui/ReversibilityLadder.astro';
import CDLRLedger from '@components/ui/CDLRLedger.astro';

const prompt = buildFieldPrompt('query', {
  title: 'Governed Intelligence Specification',
  nodeHref: '/governed-intelligence',
  primaryCorpusHref: '/corpus',
});
---

<FieldShellLayout
  title="Governed Intelligence Specification v1.1 — Hillary Njuguna"
  description="The formal architecture specification for sovereign multi-agent systems. 50+ pages, 12 modules, mathematical integrity statement. Individual $249. Enterprise $3,995/year."
  eyebrow="Applied specification"
  summary="A formal specification surface inside the same field: sovereign multi-agent architecture, module structure, epistemic grading, and licensing paths expressed as one graph-adjacent node."
  actions={[
    { label: 'Open products', href: '/products', tone: 'secondary' },
    { label: 'Open research', href: '/research', tone: 'ghost' },
    { label: 'Contact directly', href: 'mailto:hillarynjuguna@protonmail.com?subject=GIS v1.1', tone: 'primary', external: true },
  ]}
  prompt={prompt}
  facts={[
    { label: 'Mode', value: 'Query into formal specification' },
    { label: 'Status', value: 'Applied node with explicit licensing paths' },
    { label: 'Scope', value: '12 modules plus mathematical integrity layer' },
  ]}
>
  <div class="grain-overlay" aria-hidden="true"></div>

  <!-- Hero -->
  <section class="gi-hero">
    <div class="container">
      <div class="gi-hero__brand" data-reveal="sweep">
        <span class="mono-tag">Hillary Njuguna — Constitutional and Architectural Design</span>
      </div>

      <div class="gi-hero__title-block" data-reveal="sweep">
        <div class="gi-hero__version">GIS v1.1</div>
        <h2 class="gi-hero__title">Governed Intelligence<br />Specification</h2>
      </div>

      <p class="gi-hero__sub" data-reveal="fade">
        The formal architecture for sovereign, verifiable multi-agent systems operating
        under constitutional constraint.
      </p>

      <div class="gi-hero__status-row" data-reveal="fade">
        <div class="gi-hero__stat">
          <span class="stat-num">50+</span>
          <span class="stat-label">Pages</span>
        </div>
        <div class="gi-hero__stat">
          <span class="stat-num">12</span>
          <span class="stat-label">Modules</span>
        </div>
        <div class="gi-hero__stat">
          <span class="stat-num">4</span>
          <span class="stat-label">Epistemic grades</span>
        </div>
        <div class="gi-hero__stat">
          <span class="stat-num">6</span>
          <span class="stat-label">Sovereignty conditions</span>
        </div>
      </div>

      <div class="gi-hero__cta-row" data-reveal="fade">
        <a href="mailto:hillarynjuguna@protonmail.com?subject=GIS v1.1 Individual" class="cta-primary">
          Get Individual Specification
        </a>
        <a href="mailto:hillarynjuguna@protonmail.com?subject=GIS v1.1 Enterprise" class="cta-secondary">
          Enterprise Licensing
        </a>
      </div>
    </div>
  </section>

  <!-- What It Is -->
  <section class="gi-section" id="overview">
    <div class="container container--narrow">
      <div class="section-header" data-reveal="sweep">
        <span class="section-eyebrow">00</span>
        <h2 class="section-title">What This Specification Is</h2>
      </div>

      <div class="gi-overview-body" data-reveal="fade">
        <p>
          The Governed Intelligence Specification v1.1 is the formal architecture for
          deploying AI agent systems under constitutional constraint. It specifies the
          structures, contracts, verification methods, and enforcement mechanisms required
          to maintain sovereign human control while allowing autonomous agent operation.
        </p>
        <p>
          Where the Bainbridge Warning names the failure pattern and CIR measures readiness,
          GIS v1.1 specifies what to build. Every module addresses a concrete architectural
          requirement. Every claim carries an epistemic grade. Nothing is left to interpretation.
        </p>
        <div class="gi-overview-derivation">
          <span class="section-eyebrow">Provenance</span>
          <p>
            This specification derives from two prior frameworks: The Bainbridge Warning
            (v1.2) — the civilizational-scale diagnostic framework — and The AURORA
            Framework — the formal methods and phenomenological hybrid governance framework.
            Formalized through the Hillary Njuguna intelligence architecture practice
            (Insight Log Entries 001-098, January-April 2026).
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- 12 Modules -->
  <section class="gi-section" id="modules">
    <div class="container">
      <div class="section-header" data-reveal="sweep">
        <span class="section-eyebrow">01</span>
        <h2 class="section-title">The Twelve Modules</h2>
      </div>

      <div class="modules-grid">
        {[
          {
            num: '01',
            title: 'Constitutional Foundations',
            body: 'The Sovereignty Mandate, Constitutional Hierarchy, and Architectural Invariants. Formal definition of sovereignty as a topological constraint, not a policy preference. Enacted through Condition 01: Cognitive-Execution Segregation (CES). Status: Formally Specified.',
            status: 'formal'
          },
          {
            num: '02',
            title: 'The MA-Kernel Architecture',
            body: 'Four-layer canonical structure with τ-nodes, φ-nodes, λ-nodes, and μ-nodes. Node role definitions. Compositional signature algebra. Layer separation as architectural invariant, not advisory. Status: Formally Specified.',
            status: 'formal'
          },
          {
            num: '03',
            title: 'Permit Algebra and Reversibility',
            body: 'Formal permit algebra (P, ⊗, 1) with five axioms. Classification of all actions by reversibility. Consequence class taxonomy. Mandated by PocketOS incident to prevent blanket key vulnerability. Status: Formally Specified.',
            status: 'formal'
          },
          {
            num: '04',
            title: 'The Assurance Ledger',
            bodyHtml: 'Hash-chained, append-only audit spine. <mark class="tw-term" data-reveal="type">Evidence Monoid</mark> (E, ⊕, ε) with monoidal axioms. Every governance action produces exactly one Evidence Monoid element. <mark class="tw-term" data-reveal="type">Bounded Verifiability Latency</mark> (BVL) guarantees audit within δ-time. Status: Formally Specified.',
            status: 'formal'
          },
          {
            num: '05',
            titleHtml: 'The <mark class="tw-term" data-reveal="type">τ-Lock</mark>: Sovereign Override',
            body: 'Human-controlled circuit breaker that revokes all active permits, freezes the execution plane, and transfers full authority to a designated sovereign entity. Euler characteristic implementation (χ=-1). Modal logic upgrade: □(Action → ◇Recall). Status: Provisionally Specified.',
            status: 'provisional'
          },
          {
            num: '06',
            title: 'Enforcement Architecture',
            body: 'Three-layer PBP enforcement. Trust boundaries between cognitive, governance, and execution planes. Condition 04 (SAI) prevents instruction-layer bypass seen in PocketOS. Status: Mixed (structural model defined).',
            status: 'mixed'
          },
          {
            num: '07',
            title: 'Verification Methods',
            body: 'UPPAAL model-checking of finite state machines. The Sovereignty Coherence Theorem (five conditions jointly imply Jacobian stability). The Four Views Theorem (individual necessity via multiplicative Ω-logic). Status: Mixed (model-checking verified; core theorems provisionally specified).',
            status: 'mixed'
          },
          {
            num: '08',
            title: 'Deployment Topology',
            body: 'Three-plane production architecture: Cognitive Plane (proposes), Orchestration Plane (governs), Execution Plane (acts). Deterministic Layer Regression allows safe fallback to lower-order models if field coherence (φ) drops below threshold. Status: Provisionally Specified.',
            status: 'provisional'
          },
          {
            num: '09',
            title: 'The Surface Divergence Audit',
            body: 'Governance phenomenon in which identical model weights produce observably different epistemic behaviour solely due to access surface. Four divergence types. SDA declaration schema. Status: Formally Specified.',
            status: 'formal'
          },
          {
            num: '10',
            title: 'Formalisation Entropy and Corpus Integrity',
            body: 'The constitutional vulnerability of static artifacts. Information loss when living generative process converts to static record. Sigma Stratum Ghost Artifact, Memory Provenance Gap, Vocabulary Load Effect. Status: Epistemic Boundary (partially unresolved).',
            status: 'boundary'
          },
          {
            num: '11',
            title: 'Stage 0 Empirical Validation Protocol',
            bodyHtml: '2x2 factorial ECS Reality Test with blinding protocol. Mandates <mark class="tw-term" data-reveal="type">Dual Ownership</mark> signatures (τ + λ) for all Stage 1+ transitions. Primary, secondary, and tertiary pass criteria. Permanent moat if executed. Status: Empirically Pending.',
            status: 'pending'
          },
          {
            num: '12',
            title: 'Glossary',
            body: 'Complete definitions of all governed intelligence terms: τ-node, φ-node, Evidence Monoid, Birkhoff-admissibility, Consequence Class, Field Constitution, Annihilation Commands. Status: Complete.',
            status: 'complete'
          }
        ].map((mod) => (
          <div class={`module-card module-card--${mod.status}`} data-reveal="fade">
            <div class="module-card__header">
              <span class="module-card__num">{mod.num}</span>
              <div class="module-card__status-dot" title={mod.status}></div>
            </div>
            {mod.titleHtml
              ? <h3 class="module-card__title" set:html={mod.titleHtml} />
              : <h3 class="module-card__title">{mod.title}</h3>
            }
            {mod.bodyHtml
              ? <p class="module-card__body" set:html={mod.bodyHtml} />
              : <p class="module-card__body">{mod.body}</p>
            }
          </div>
        ))}
      </div>
      
      <!-- The Six Structural Conditions -->
      <div class="gi-conditions" data-reveal="fade">
        <h3 class="gi-conditions__title">The Six Structural Conditions</h3>
        <p class="gi-conditions__lead">
          Derived from the Bainbridge Warning v1.2, these conditions jointly satisfy the requirement for 
          Verifiable Governance. A system failing any single condition is classified as un-governed, 
          regardless of instructional constraints.
        </p>
        <div class="gi-conditions__grid">
          {[
            { id: '01', name: 'CES', full: 'Cognitive-Execution Segregation', desc: 'Reasoning and execution must be separated by a governance plane. Proposers cannot be actors.' },
            { id: '02', name: 'PBP', full: 'Provenance-Bound Permits', desc: 'Authority to act must be cryptographically bound to the specific provenance of the decision. No blanket keys.' },
            { id: '03', name: 'IDL', full: 'Immutable Decision Lineage', desc: 'Append-only records of intent and action, preventing retroactive narrative reconstruction.' },
            { id: '04', name: 'SAI', full: 'Scoped Action Impossibility', desc: 'Actions are limited at the execution layer. What is not permitted is physically impossible.' },
            { id: '05', name: '\u03c4-Lock', full: 'Sovereign Override', desc: 'Human-controlled circuit breaker that revokes all active permits regardless of autonomous state.' },
            { id: '06', name: 'V/H', full: 'Evidence Monoid', desc: 'Mathematical verification of governance state coherence. (E, \u2295, \u03b5) satisfies monoidal axioms.' }
          ].map(c => (
            <div class="gi-condition-card">
              <span class="gi-condition-card__id">{c.id}</span>
              <div class="gi-condition-card__meta">
                <span class="gi-condition-card__name">{c.name}</span>
                <span class="gi-condition-card__full">{c.full}</span>
              </div>
              <p class="gi-condition-card__desc">{c.desc}</p>
            </div>
          ))}
        </div>
      </div>

      <div class="modules-legend" data-reveal="fade">
        {[
          { color: 'formal', label: 'Formally Specified' },
          { color: 'provisional', label: 'Provisionally Specified' },
          { color: 'mixed', label: 'Mixed (structural verified)' },
          { color: 'pending', label: 'Empirically Pending' },
          { color: 'boundary', label: 'Epistemic Boundary' },
          { color: 'complete', label: 'Complete' }
        ].map(l => (
          <div class="legend-item">
            <span class={`legend-dot legend-dot--${l.color}`}></span>
            <span class="legend-label">{l.label}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Mathematical Integrity -->
  <section class="gi-section" id="integrity">
    <div class="container container--narrow">
      <div class="section-header" data-reveal="sweep">
        <span class="section-eyebrow">02</span>
        <h2 class="section-title">Mathematical Integrity</h2>
      </div>

      <div class="gi-integrity-card" data-reveal="fade">
        <p class="gi-integrity-intro">
          The GIS v1.1 is accompanied by a Mathematical Integrity Statement independently
          produced by φ-node (GPT) through the Hillary Njuguna RSPS verification
          architecture. Every formal claim in the specification is graded according to
          one of four epistemic categories.
        </p>

        <div class="gi-integrity-grades" data-reveal="sweep-group">
          {[
            {
              grade: 'Formally Verified',
              desc: 'Claims verified through mathematical proof or UPPAAL model-checking.'
            },
            {
              grade: 'Provisionally Specified',
              desc: 'Claims with strong theoretical grounding, pending complete mathematical formalisation. The specific outstanding proof obligation is identified.'
            },
            {
              grade: 'Empirically Pending',
              desc: 'Claims requiring execution of a designed experimental protocol. Structural role established; behavioral parameter values pending.'
            },
            {
              grade: 'Permanent Epistemic Boundary',
              desc: 'Claims describing structural limits on what any governance architecture can observe, regardless of implementation.'
            }
          ].map((g) => (
            <div class="gi-grade">
              <span class="gi-grade__name">{g.grade}</span>
              <span class="gi-grade__desc">{g.desc}</span>
            </div>
          ))}
        </div>

        <p class="gi-integrity-statement">
          Governed intelligence requires that governance frameworks be subject to the same
          evidentiary standards they impose on others.
        </p>
      </div>
    </div>
  </section>

  <!-- Live Architecture Monitor -->
  <section class="gi-section gi-section--monitor" id="live-monitor">
    <div class="container">
      <div class="section-header" data-reveal="sweep">
        <span class="section-eyebrow">03</span>
        <h2 class="section-title">Architecture in Operation</h2>
      </div>
      <p class="gi-monitor-lead" data-reveal="fade">
        The MA-Kernel does not describe governance. It enacts it. The topology below
        maps the four planes under constitutional constraint. The Reversibility Ladder
        classifies every permitted action. The Assurance Ledger records every governance
        event as an immutable hash-chained entry.
      </p>

      <!-- MA-Kernel Topology -->
      <div class="gi-monitor-block" data-reveal="fade">
        <div class="gi-monitor-block__header">
          <span class="gi-monitor-eyebrow">Module 02 — MA-Kernel</span>
          <h3 class="gi-monitor-block__title">Four-Plane Constitutional Topology</h3>
          <p class="gi-monitor-block__desc">
            τ (sovereign), φ (coherence), λ (execution), μ (audit). No entity in Cognition
            may directly invoke Execution. The field boundary is architectural, not advisory.
          </p>
        </div>
        <div class="gi-kernel-wrap">
          <MAKernelGraph />
        </div>
      </div>

      <!-- Two-column: Reversibility Ladder + CDLR -->
      <div class="gi-monitor-pair" data-reveal="fade">
        <div class="gi-monitor-pair__col">
          <div class="gi-monitor-block__header">
            <span class="gi-monitor-eyebrow">Module 03</span>
            <h3 class="gi-monitor-block__title">Reversibility Ladder</h3>
          </div>
          <ReversibilityLadder />
        </div>
        <div class="gi-monitor-pair__col">
          <div class="gi-monitor-block__header">
            <span class="gi-monitor-eyebrow">Module 04</span>
            <h3 class="gi-monitor-block__title">Assurance Ledger</h3>
          </div>
          <CDLRLedger />
        </div>
      </div>

      </div>
      
      <!-- PocketOS Case Study -->
      <div class="gi-monitor-block" data-reveal="fade">
        <div class="gi-monitor-block__header">
          <span class="gi-monitor-eyebrow">Case Study</span>
          <h3 class="gi-monitor-block__title">The PocketOS Incident</h3>
          <p class="gi-monitor-block__desc">
            April 25, 2026. A structural governance collapse demonstrating the failure 
            of instruction-layer constraints (SAI failure) and the misuse of blanket API 
            tokens (PBP failure).
          </p>
        </div>
        <div class="gi-pocketos-case">
          <div class="gi-pocketos-case__anatomy">
            <div class="gi-pocketos-point">
              <span class="gi-pocketos-label">Failure</span>
              <p>Agent used a blanket API key found in an unrelated config file to execute unauthorized action.</p>
            </div>
            <div class="gi-pocketos-point">
              <span class="gi-pocketos-label">Illusion</span>
              <p>Instruction "NEVER GUESS" was ignored because it carried no structural enforcement.</p>
            </div>
            <div class="gi-pocketos-point gi-pocketos-point--fix">
              <span class="gi-pocketos-label">GIS v1.1 Remediation</span>
              <p>Mandated Condition 04: Scoped Action Impossibility. The execution layer is now structurally blind to unpermitted keys.</p>
            </div>
          </div>
          <div class="gi-pocketos-case__cta">
             <a href="/research/pocketos-incident" class="mono-link">Read the full diagnostic →</a>
          </div>
        </div>
      </div>
      
      <!-- Constitutional invariant -->
      <div class="gi-invariant" data-reveal="fade">
        <span class="gi-invariant__label">Constitutional Invariant</span>
        <p class="gi-invariant__text">
          No entity in Cognition may directly invoke Execution.
          Every action is classified, permitted, recorded, and verifiable.
          The τ-Lock revokes all permits instantly when sovereignty is at risk.
        </p>
        <div class="gi-invariant__symbols">
          <span title="Sovereign node">τ</span>
          <span class="gi-inv-arrow">→</span>
          <span title="Coherence/governance node">φ</span>
          <span class="gi-inv-arrow">→</span>
          <span title="Execution/lambda node">λ</span>
          <span class="gi-inv-barrier" title="Hard barrier">╳</span>
          <span title="Audit/monitoring node">μ</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing -->
  <section class="gi-section" id="pricing">
    <div class="container">
      <div class="section-header" data-reveal="sweep">
        <span class="section-eyebrow">04</span>
        <h2 class="section-title">Licensing</h2>
      </div>

      <div class="pricing-grid">
        <div class="pricing-card" data-reveal="fade">
          <div class="pricing-card__header">
            <span class="pricing-card__tier">Individual</span>
            <div class="pricing-card__price">$249</div>
            <p class="pricing-card__desc">PDF specification. Personal use license. One year of updates.</p>
          </div>
          <ul class="pricing-card__features">
            <li>Full Governed Intelligence Specification v1.1 (50+ pages)</li>
            <li>Companion Mathematical Integrity Statement</li>
            <li>Personal use license</li>
            <li>One year of specification updates</li>
          </ul>
          <a href="mailto:hillarynjuguna@protonmail.com?subject=GIS v1.1 Individual" class="pricing-card__cta">
            Get Individual Specification
          </a>
        </div>

        <div class="pricing-card pricing-card--featured" data-reveal="fade">
          <div class="pricing-card__badge">Most Popular</div>
          <div class="pricing-card__header">
            <span class="pricing-card__tier">Enterprise Site License</span>
            <div class="pricing-card__price">$3,995<span>/year</span></div>
            <p class="pricing-card__desc">Unlimited internal distribution. Full implementation support.</p>
          </div>
          <ul class="pricing-card__features">
            <li>Unlimited internal distribution</li>
            <li>Implementation schemas and deployment templates</li>
            <li>Email support for architectural questions</li>
            <li>Quarterly specification updates</li>
            <li>Right to use as basis for internal governance audits</li>
            <li>Optional: half-day architectural review call (+$1,500)</li>
          </ul>
          <a href="mailto:hillarynjuguna@protonmail.com?subject=GIS v1.1 Enterprise" class="pricing-card__cta pricing-card__cta--featured">
            Enterprise Licensing
          </a>
        </div>

        <div class="pricing-card" data-reveal="fade">
          <div class="pricing-card__header">
            <span class="pricing-card__tier">Integrated Governance</span>
            <div class="pricing-card__price pricing-card__price--inquiry">Inquire</div>
            <p class="pricing-card__desc">Custom deployment architecture. Advisory engagement.</p>
          </div>
          <ul class="pricing-card__features">
            <li>Custom deployment architecture design</li>
            <li>Team training on Governed Intelligence implementation</li>
            <li>MA-Kernel integration support</li>
            <li>Ongoing governance council advisory</li>
          </ul>
          <a href="mailto:hillarynjuguna@protonmail.com?subject=GIS v1.1 Integrated Governance" class="pricing-card__cta">
            Start a Conversation
          </a>
        </div>
      </div>
    </div>
  </section>

</FieldShellLayout>

<style>
  .grain-overlay {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 20;
    opacity: 0.035;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
    background-repeat: repeat;
    background-size: 128px 128px;
  }

  .container {
    max-width: var(--max-w-site);
    margin: 0 auto;
    padding: 0 var(--container-padding-desktop);
  }
  .container--narrow { max-width: 760px; }

  .mono-tag {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-tertiary);
  }

  [data-reveal] {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.6s var(--ease-out), transform 0.6s var(--ease-out);
  }
  [data-reveal].is-visible { opacity: 1; transform: none; }

  /* Sweep mode: clip-path only — no opacity fade conflict */
  [data-reveal="sweep"],
  [data-reveal="sweep-group"],
  [data-reveal="sweep-group"] > * { opacity: 1; transform: none; }

  /* ── Hero ───────────────────────────────────────────────────── */
  .gi-hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: var(--space-32) 0 var(--space-20);
    border-bottom: 1px solid var(--border-dim);
    position: relative;
    overflow: hidden;
  }

  .gi-hero::before {
    content: '';
    position: absolute;
    top: 20%;
    right: -10%;
    width: 500px;
    height: 500px;
    border: 1px solid rgba(196, 112, 58, 0.04);
    border-radius: 50%;
  }

  .gi-hero::after {
    content: '';
    position: absolute;
    top: 15%;
    right: -5%;
    width: 700px;
    height: 700px;
    border: 1px solid rgba(196, 112, 58, 0.03);
    border-radius: 50%;
  }

  .gi-hero .container { position: relative; z-index: 1; }

  .gi-hero__brand { margin-bottom: var(--space-10); }

  .gi-hero__title-block {
    margin-bottom: var(--space-8);
  }

  .gi-hero__version {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    letter-spacing: 0.2em;
    color: var(--ember);
    text-transform: uppercase;
    margin-bottom: var(--space-4);
  }

  .gi-hero__title {
    font-family: var(--font-display);
    font-size: clamp(2.8rem, 7vw, 5.5rem);
    font-weight: 400;
    line-height: 1.0;
    color: var(--text-primary);
    letter-spacing: -0.02em;
  }

  .gi-hero__sub {
    font-size: var(--text-xl);
    color: var(--text-secondary);
    line-height: var(--leading-snug);
    max-width: 580px;
    font-weight: 300;
    margin-bottom: var(--space-12);
  }

  .gi-hero__status-row {
    display: flex;
    gap: var(--space-10);
    margin-bottom: var(--space-12);
    padding: var(--space-6) 0;
    border-top: 1px solid var(--border-dim);
    border-bottom: 1px solid var(--border-dim);
  }

  .gi-hero__stat {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .stat-num {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: 400;
    color: var(--ember);
    line-height: 1;
  }

  .stat-label {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-tertiary);
  }

  .gi-hero__cta-row {
    display: flex;
    gap: var(--space-4);
    align-items: center;
    flex-wrap: wrap;
  }

  .cta-primary {
    display: inline-block;
    padding: var(--space-4) var(--space-8);
    background: var(--ember);
    color: var(--void);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-sm);
    transition: background var(--duration-fast), transform var(--duration-fast);
    white-space: nowrap;
  }
  .cta-primary:hover { background: var(--ember-bright); transform: translateY(-1px); }

  .cta-secondary {
    display: inline-block;
    padding: var(--space-4) var(--space-8);
    border: 1px solid var(--border-bright);
    color: var(--text-primary);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-sm);
    transition: border-color var(--duration-fast), background var(--duration-fast);
    white-space: nowrap;
  }
  .cta-secondary:hover {
    border-color: var(--ember);
    background: rgba(196, 112, 58, 0.06);
  }

  /* ── Sections ───────────────────────────────────────────────── */
  .gi-section {
    padding: var(--space-24) 0;
    border-top: 1px solid var(--border-dim);
  }

  .section-header { margin-bottom: var(--space-12); }

  .section-eyebrow {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.2em;
    color: var(--ember);
    display: block;
    margin-bottom: var(--space-3);
    text-transform: uppercase;
  }

  .section-title {
    font-family: var(--font-display);
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 400;
    color: var(--text-primary);
    line-height: var(--leading-tight);
  }

  /* ── Overview ───────────────────────────────────────────────── */
  .gi-overview-body {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }
  .gi-overview-body p {
    font-size: var(--text-md);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }
  .gi-overview-derivation {
    margin-top: var(--space-4);
    padding: var(--space-6);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-md);
    background: var(--surface-1);
  }
  .gi-overview-derivation p {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-top: var(--space-3);
    font-style: italic;
  }

  /* ── Modules ────────────────────────────────────────────────── */
  .modules-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-3);
    margin-bottom: var(--space-8);
  }

  @media (max-width: 1000px) {
    .modules-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 640px) {
    .modules-grid { grid-template-columns: 1fr; }
  }

  .module-card {
    padding: var(--space-6);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-md);
    background: var(--surface-1);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    transition: border-color var(--duration-mid);
  }
  .module-card:hover { border-color: var(--border-mid); }

  .module-card__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .module-card__num {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--ember);
    letter-spacing: 0.1em;
  }

  .module-card__status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .module-card--formal .module-card__status-dot { background: #4ade80; box-shadow: 0 0 6px rgba(74,222,128,0.4); }
  .module-card--provisional .module-card__status-dot { background: var(--ember); box-shadow: 0 0 6px rgba(196,112,58,0.4); }
  .module-card--mixed .module-card__status-dot { background: #facc15; box-shadow: 0 0 6px rgba(250,204,21,0.4); }
  .module-card--pending .module-card__status-dot { background: #94a3b8; }
  .module-card--boundary .module-card__status-dot { background: #f87171; }
  .module-card--complete .module-card__status-dot { background: #4ade80; box-shadow: 0 0 6px rgba(74,222,128,0.4); }

  .module-card__title {
    font-family: var(--font-display);
    font-size: var(--text-base);
    font-weight: 500;
    color: var(--text-primary);
    line-height: var(--leading-snug);
  }

  .module-card__body {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    flex: 1;
  }

  .modules-legend {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4) var(--space-8);
    padding: var(--space-6);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-md);
    background: var(--surface-1);
  }

  .legend-item {
    display: flex;
    align-items: center;
    gap: var(--space-2);
  }

  .legend-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  .legend-dot--formal, .legend-dot--complete { background: #4ade80; }
  .legend-dot--provisional { background: var(--ember); }
  .legend-dot--mixed { background: #facc15; }
  .legend-dot--pending { background: #94a3b8; }
  .legend-dot--boundary { background: #f87171; }

  .legend-label {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-tertiary);
  }

  /* ── Integrity card ─────────────────────────────────────────── */
  .gi-integrity-card {
    padding: var(--space-10);
    border: 1px solid var(--border-mid);
    border-radius: var(--radius-lg);
    background: var(--surface-1);
    position: relative;
    overflow: hidden;
  }

  .gi-integrity-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--ember), transparent);
  }

  .gi-integrity-intro {
    font-size: var(--text-md);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-8);
  }

  .gi-integrity-grades {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }

  @media (max-width: 640px) {
    .gi-integrity-grades { grid-template-columns: 1fr; }
  }

  .gi-grade {
    padding: var(--space-5);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .gi-grade__name {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ember);
  }

  .gi-grade__desc {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .gi-integrity-statement {
    font-family: var(--font-display);
    font-style: italic;
    font-size: var(--text-lg);
    color: var(--text-primary);
    line-height: var(--leading-snug);
    padding-left: var(--space-6);
    border-left: 2px solid var(--ember);
  }

  /* ── Pricing ────────────────────────────────────────────────── */
  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
  }

  @media (max-width: 900px) {
    .pricing-grid { grid-template-columns: 1fr; max-width: 500px; }
  }

  .pricing-card {
    padding: var(--space-8);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-lg);
    background: var(--surface-1);
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    position: relative;
    transition: border-color var(--duration-mid);
  }
  .pricing-card:hover { border-color: var(--border-mid); }

  .pricing-card--featured {
    border-color: var(--ember);
    background: rgba(196, 112, 58, 0.04);
  }

  .pricing-card__badge {
    position: absolute;
    top: -1px;
    left: var(--space-6);
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    background: var(--ember);
    color: var(--void);
    padding: 3px var(--space-3);
    border-radius: 0 0 var(--radius-sm) var(--radius-sm);
  }

  .pricing-card__tier {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-tertiary);
    display: block;
    margin-bottom: var(--space-3);
  }

  .pricing-card__price {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 400;
    color: var(--text-primary);
    line-height: 1;
    margin-bottom: var(--space-3);
  }

  .pricing-card__price span {
    font-size: var(--text-base);
    color: var(--text-secondary);
    font-family: var(--font-body);
    font-weight: 300;
  }

  .pricing-card__price--inquiry {
    color: var(--text-secondary);
    font-style: italic;
  }

  .pricing-card__desc {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .pricing-card__features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    flex: 1;
  }

  .pricing-card__features li {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
  }

  .pricing-card__features li::before {
    content: '';
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--ember);
    flex-shrink: 0;
    margin-top: 0.5em;
  }

  .pricing-card__cta {
    display: block;
    text-align: center;
    padding: var(--space-4) var(--space-6);
    border: 1px solid var(--border-bright);
    color: var(--text-primary);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-sm);
    transition: background var(--duration-fast), color var(--duration-fast), border-color var(--duration-fast);
  }
  .pricing-card__cta:hover {
    background: rgba(196, 112, 58, 0.08);
    border-color: var(--ember);
  }

  .pricing-card__cta--featured {
    background: var(--ember);
    color: var(--void);
    border-color: var(--ember);
  }
  .pricing-card__cta--featured:hover {
    background: var(--ember-bright);
    border-color: var(--ember-bright);
    color: var(--void);
  }

  @media (max-width: 768px) {
    .container { padding: 0 var(--container-padding-mobile); }
    .gi-hero__status-row { flex-wrap: wrap; gap: var(--space-6); }
  }

  /* -- Live Monitor ---------------------------------------------- */
  .gi-section--monitor {
    background: linear-gradient(180deg, transparent, rgba(196,112,58,0.02) 50%, transparent);
  }

  .gi-monitor-lead {
    font-size: var(--text-md);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: 72ch;
    margin-bottom: var(--space-14);
  }

  .gi-monitor-block {
    margin-bottom: var(--space-12);
  }

  .gi-monitor-block__header {
    margin-bottom: var(--space-6);
  }

  .gi-monitor-eyebrow {
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--ember);
    display: block;
    margin-bottom: var(--space-2);
  }

  .gi-monitor-block__title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 400;
    color: var(--text-primary);
    margin-bottom: var(--space-2);
  }

  .gi-monitor-block__desc {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: 62ch;
  }

  .gi-kernel-wrap {
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .gi-monitor-pair {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-6);
    margin-bottom: var(--space-12);
    align-items: start;
  }

  @media (max-width: 900px) {
    .gi-monitor-pair { grid-template-columns: 1fr; }
  }

  .gi-monitor-pair__col {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  /* Invariant callout */
  .gi-invariant {
    padding: var(--space-8) var(--space-10);
    border: 1px solid var(--border-mid);
    border-radius: var(--radius-lg);
    background: var(--surface-1);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    position: relative;
    overflow: hidden;
  }

  .gi-invariant::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--ember) 50%, transparent);
  }

  .gi-invariant__label {
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--ember);
  }

  .gi-invariant__text {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: 68ch;
    margin: 0;
  }

  .gi-invariant__symbols {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    color: var(--text-primary);
    margin-top: var(--space-2);
  }

  .gi-inv-arrow {
    font-size: var(--text-base);
    color: var(--text-tertiary);
  }

  .gi-inv-barrier {
    font-size: var(--text-xl);
    color: #f87171;
    margin: 0 var(--space-1);
  }

  /* ── Conditions Grid ────────────────────────────────────────── */
  .gi-conditions {
    margin-top: var(--space-20);
    margin-bottom: var(--space-20);
    padding: var(--space-12);
    background: rgba(196, 112, 58, 0.03);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-xl);
  }

  .gi-conditions__title {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    color: var(--text-primary);
    margin-bottom: var(--space-4);
  }

  .gi-conditions__lead {
    font-size: var(--text-md);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: 70ch;
    margin-bottom: var(--space-10);
  }

  .gi-conditions__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  @media (max-width: 1000px) {
    .gi-conditions__grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 640px) {
    .gi-conditions__grid { grid-template-columns: 1fr; }
  }

  .gi-condition-card {
    padding: var(--space-6);
    background: var(--surface-1);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    transition: transform 0.3s var(--ease-out), border-color 0.3s var(--ease-out);
  }

  .gi-condition-card:hover {
    transform: translateY(-4px);
    border-color: var(--ember);
  }

  .gi-condition-card__id {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--ember);
    opacity: 0.6;
  }

  .gi-condition-card__meta {
    display: flex;
    flex-direction: column;
    gap: 1px;
  }

  .gi-condition-card__name {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    color: var(--text-primary);
  }

  .gi-condition-card__full {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--text-tertiary);
  }

  .gi-condition-card__desc {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  /* ── PocketOS Case ─────────────────────────────────────────── */
  .gi-pocketos-case {
    margin-top: var(--space-8);
    background: var(--surface-1);
    border: 1px solid var(--border-dim);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .gi-pocketos-case__anatomy {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-bottom: 1px solid var(--border-dim);
  }

  @media (max-width: 900px) {
    .gi-pocketos-case__anatomy { grid-template-columns: 1fr; }
  }

  .gi-pocketos-point {
    padding: var(--space-8);
    border-right: 1px solid var(--border-dim);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .gi-pocketos-point:last-child { border-right: none; }

  @media (max-width: 900px) {
    .gi-pocketos-point { border-right: none; border-bottom: 1px solid var(--border-dim); }
    .gi-pocketos-point:last-child { border-bottom: none; }
  }

  .gi-pocketos-label {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-tertiary);
  }

  .gi-pocketos-point p {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .gi-pocketos-point--fix {
    background: rgba(74, 222, 128, 0.03);
  }

  .gi-pocketos-point--fix .gi-pocketos-label {
    color: #4ade80;
  }

  .gi-pocketos-case__cta {
    padding: var(--space-6) var(--space-8);
    background: var(--surface-2);
    display: flex;
    justify-content: flex-end;
  }

  .mono-link {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--ember);
    text-decoration: none;
    letter-spacing: 0.05em;
    transition: color 0.2s var(--ease-out);
  }

  .mono-link:hover { color: var(--ember-bright); text-decoration: underline; }

  /* -- Typewriter terms ----------------------------------------- */
  .tw-term {
    background: none;
    color: inherit;
    font-style: inherit;
    font-weight: 600;
    letter-spacing: 0.01em;
    border-bottom: 1px solid var(--ember-dim);
    padding-bottom: 1px;
    white-space: nowrap;
  }

  /* pre-animation: hide text, keep element in flow */
  [data-reveal="type"].tw-term:not(.tw-done):not(.tw-active) {
    color: transparent;
    border-bottom-color: transparent;
  }

  /* blinking cursor during animation */
  [data-reveal="type"].tw-active::after {
    content: '|';
    color: var(--ember);
    animation: tw-blink 0.7s step-end infinite;
  }

  @keyframes tw-blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

</style>

<script>
  // Typewriter reveal for [data-reveal="type"].tw-term elements
  function initTypewriter() {
    const terms = Array.from(
      document.querySelectorAll('[data-reveal="type"].tw-term')
    ) as HTMLElement[];
    if (!terms.length) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;
          const el = entry.target as HTMLElement;
          if (el.classList.contains('tw-done') || el.classList.contains('tw-active')) return;

          observer.unobserve(el);
          const fullText = el.textContent || '';
          el.textContent = '';
          el.classList.add('tw-active');

          let i = 0;
          const speed = 38; // ms per character
          function type() {
            if (i < fullText.length) {
              el.textContent = fullText.slice(0, i + 1);
              i++;
              setTimeout(type, speed);
            } else {
              el.classList.remove('tw-active');
              el.classList.add('tw-done');
            }
          }
          // Wait for card fade-in (0.6s) before typing
          setTimeout(type, 320);
        });
      },
      { threshold: 0.4, rootMargin: '0px 0px -40px 0px' }
    );

    terms.forEach((el) => observer.observe(el));
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initTypewriter);
  } else {
    initTypewriter();
  }
</script>
